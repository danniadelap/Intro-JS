var numero = Number(prompt("Ingrese un numero"));

if (numero === 1000) {
    console.log("Ganaste un premio")
} else {
    console.log("El numero correcto es 1000. Lo sentimos, sigue participando." )
}